sudo: false
language: cpp
compiler:
  - gcc-4.8
  - clang-3.4
env:
  - SUITE=DGLM_TEST_ENABLE_CXX_98=ON
  - SUITE=DGLM_TEST_ENABLE_CXX_0X=ON

script: $CXX -v;mkdir ./build; cd ./build; cmake -DGLM_TEST_ENABLE=ON $SUITE ..; cmake --build .; ctest
